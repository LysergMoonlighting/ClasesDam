package com.parkingGestion;

import java.util.ArrayList;

import javax.swing.JOptionPane;


public class parking {
	
	
	
	
	public static void menu() {
		
		int resp=-1;
		do {
				resp= imprimirMenu(resp);
				if (resp==0) {
					
					
				}else if(resp==1) {
					
				}else if(resp==2) {
					
				}else if(resp==3) {
					
				}
				else {
					System.out.println("ERROR, NO SE HA ENCONTRADO LA OPCION");
				}
					
				 
				

			
		}while(resp!=3);
	}

	private static int imprimirMenu(int resp){
        String [] opciones = {"Añadir plaza","Sacar coche","Buscar vehiculo","Salir"};
        resp= JOptionPane.showOptionDialog(null,"Seleccione la opción que desea efectuar" , "Menu" , JOptionPane.DEFAULT_OPTION, JOptionPane.PLAIN_MESSAGE, null, opciones, null);
        return resp;

    }
}
